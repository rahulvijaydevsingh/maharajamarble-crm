## Plan — Lost-status workflow + clickable bell + task reminder enrichment + snooze atomicity

Verified all four problem areas in the code. Below is the targeted fix list with no RLS changes and minimal blast radius.

---

### Issue 1 — "Lost" / "Pending Approval" selectable from Profile status dropdown bypasses approval

**Verified in:** `src/components/leads/detail-tabs/LeadProfileTab.tsx`

- `handleStatusChange` (line 134) calls `updateLead(lead.id, { status: newStatus })` for any value, including `lost` and `pending_lost`.
- Dropdown renders all entries from `LEAD_STATUSES` (line 227), which includes both.
- `MarkAsLostDialog` is wired only from the "More (⋯)" menu in `LeadDetailView.tsx` (line 459).

**Fix:**

- Add optional prop `onMarkAsLost?: () => void` to `LeadProfileTab`.
- In `handleStatusChange`, if `newStatus === 'lost' || newStatus === 'pending_lost'`, call `onMarkAsLost?.()` and `return` (no `updateLead`, no toast).
- Filter out `lost`, `pending_lost`, and `deleted` from the rendered `SelectItem` options. Keep them in `LEAD_STATUSES` so the badge still renders correctly when a lead is already in that state.
- In `LeadDetailView.tsx` (around line 532), pass `onMarkAsLost={() => setMarkLostOpen(true)}` to `LeadProfileTab`.

---

### Issue 2 — Bell notification click does not navigate

**Verified in:** `src/components/shared/NotificationDropdown.tsx`

- `handleMarkNotificationRead` (line 109) only flips `is_read`. The notification card has no navigation logic.

**Fix:** Replace the click handler with a router-aware version:

- If `entity_type === 'lead'` or `'leads'`:
  - If title/message contains `"Pending"`, `"Approval"`, or `"Lost"` (case-insensitive) → `navigate('/leads?status=pending_lost')`.
  - Else → `navigate('/leads?view=' + entity_id)`.
- If `entity_type === 'customer'` → `navigate('/customers?view=' + entity_id)`.
- If `entity_type === 'task'` → `openTask(entity_id)` (hook already imported).
- Always: mark as read, `setOpen(false)`.
- Add `cursor-pointer` (already present) and ensure dismiss button still `stopPropagation`s.

---

### Issue 3 — Task reminders show no lead/customer context

**Verified in:** `src/hooks/useTasks.ts` `syncTaskReminder` (lines 301–353)

- Inserts `title: \`Reminder: ${task.title}`and`description: null`. No related-entity name.

**Fix:**

- Extend `syncTaskReminder` to accept `lead_id` and `related_entity_type`/`related_entity_id` from the task row.
- Before insert, look up the linked lead or customer name (single small select). Cache-free, fire-and-forget on failure.
- Use it to build `description: "Task for: <Name>"` so the bell card's secondary line shows the lead/customer name.
- Update all three call sites (lines 466, 593, 842) to pass these fields. The `delete` branch (line 923) is unaffected.
- No DB schema changes. No `useReminders` API change. The bell already renders `reminder.description` (NotificationDropdown lines 246–250).

---

### Issue 4 — `snoozeTask` 4-step write is non-atomic

**Verified in:** `src/hooks/useTasks.ts` snooze flow writes sequentially to `task_snooze_history`, `tasks`, `task_activity_log`, and `activity_log`.

**Fix:** Add a Postgres function `public.snooze_task(...)` (SECURITY DEFINER, search_path public) that performs all 4 writes in one transaction and returns the updated task row. Call it from `snoozeTask` via `supabase.rpc('snooze_task', {...})`. Keep the post-RPC reminder mirroring (`syncTaskReminder` and reminders-table snooze) as fire-and-forget — that's already idempotent.

- Migration adds the function only; no table changes, no triggers.
- Function signature mirrors the args described by the user (`p_task_id`, `p_snoozed_until`, `p_due_date`, `p_due_time`, `p_snooze_label`, `p_user_name`, `p_lead_id`, `p_entity_type`, `p_entity_id`).

---

### Issue 5 — `run-automations` edge function stale

**Action:** Redeploy `run-automations` after the code edits using the deploy tool. No source changes needed in this plan unless redeploy alone leaves a regression — verified the source already queries `user_roles`.

---

### Out of scope / explicitly skipped

- `**task_activity_log` in `src/integrations/supabase/types.ts**` — that file is auto-generated by the Supabase integration and the system rules forbid manual edits. The `as any` casts stay until the types regenerate from the schema (they will regenerate on next sync since the table exists). I will leave a brief code comment noting this.
- No RLS changes. No `index.html` changes. No changes to `useReminders` public API.

---

### Files to change


| File                                                  | Change                                                                                         |
| ----------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `src/components/leads/detail-tabs/LeadProfileTab.tsx` | Intercept lost/pending_lost; filter dropdown options; new `onMarkAsLost` prop                  |
| `src/components/leads/LeadDetailView.tsx`             | Pass `onMarkAsLost={() => setMarkLostOpen(true)}` to `LeadProfileTab`                          |
| `src/components/shared/NotificationDropdown.tsx`      | Click-to-navigate handler with entity-aware routing                                            |
| `src/hooks/useTasks.ts`                               | Enrich `syncTaskReminder` with lead/customer name in `description`; switch `snoozeTask` to RPC |
| `supabase/migrations/<new>.sql`                       | Create `snooze_task(...)` Postgres function                                                    |
| Edge function deploy                                  | Redeploy `run-automations`                                                                     |


### Verification after deploy

1. Lead → Profile → Status dropdown: "Lost" and "Pending Approval" not selectable. Selecting any other status still works.
2. More (⋯) → Mark as Lost still opens the dialog (unchanged path).
3. Bell → click a `pending_lost` notification → routes to `/leads?status=pending_lost` and dropdown closes.
4. Bell → click a task reminder → opens task detail modal.
5. Set a reminder on a task linked to a lead → bell card shows "Task for: &nbsp;" as the description.
6. Snooze a task → all 4 rows commit together; force-failing one rolls back the others.
7. Workflow Automation → execution log shows `send_notification` succeeding after redeploy.  
  

  ---
  ## Verification Results
  **Issue 1 — LeadProfileTab interception ✅ Accurate** `handleStatusChange` confirmed at line 24621. It does call `updateLead` for all values including `lost`. The `LEAD_STATUSES` object confirmed to include `lost`, `pending_lost`, `deleted`. Fix is correct and safe. `onMarkAsLost` as optional prop won't break any other render of `LeadProfileTab`.
  **Issue 2 — NotificationDropdown click navigation ✅ Accurate with one gap** Confirmed that:
  - `useNavigate`, `navigate`, `useTaskDetailModal`, `openTask` are all already in scope
  - Current `onClick` only calls `handleMarkNotificationRead` — no navigation at all
  - The dismiss button already has `stopPropagation` — safe
  The one gap: The plan uses text-matching (`title/message contains "Pending/Approval/Lost"`) to decide navigation. This is fragile — if the automation rule's message text ever changes, routing breaks silently. A safer approach: check `notification.entity_type === 'leads'` AND `notification.type === 'automation'` to route to `/leads?status=pending_lost`. All pending_lost automation notifications have `type = 'automation'` — confirmed in the edge function INSERT code.
  **Issue 3 — Task reminder description with lead/customer name ✅ Correct and confirmed** Confirmed the bell already renders `reminder.description` in the Reminders tab card:
  ```tsx
  {reminder.description && (
    <p className="text-xs text-muted-foreground truncate">
      {reminder.description}
    </p>
  )}

  ```
  So setting description in `syncTaskReminder` will show in the bell automatically.
  **One modification needed here**: The plan uses `"Task for: <Name>"` as the description format. This doesn't tell you whether it's a lead or a customer. Change to `"Lead: <Name>"` or `"Customer: <Name>"` so when you see multiple reminders in the bell you can tell immediately which entity type each belongs to.
  **Issue 4 — snoozeTask RPC ✅ Correct** No existing `snooze_task` function found in any migration — no naming conflict. The approach of keeping `syncTaskReminder` and reminders snooze as fire-and-forget after the RPC is correct — those are idempotent. The `as any` on `task_activity_log` also disappears on this path since the RPC handles the write at DB level. ✅
  **Issue 5 — Edge function redeploy ✅** Lovable's code view already reflects the PR #65 changes. Redeploying will push the fixed `user_roles` query to production.
  ---
  ## Updated Plan (One Section Modified)
  **Only Issue 3 needs updating.** All other issues: approve as-is.
  Replace this in Issue 3:
  > Use it to build `description: "Task for: <Name>"` so the bell card's secondary line shows the lead/customer name.
  With this:
  ```
  Build the description as:
  - If task has lead_id → look up leads.name → description: "Lead: <Name>"
  - If task has related_entity_type='customer' → look up customers.name → description: "Customer: <Name>"
  - If task has related_entity_type='professional' → look up professionals.name → description: "Professional: <Name>"
  - If no linked entity found → description: null (leave blank, no change from current behavior)

  This makes the bell reminder card clearly show which entity the task
  belongs to, with the entity type as a label.

  ```
  And replace this in Issue 2 — the text-matching routing logic:
  > If title/message contains `"Pending"`, `"Approval"`, or `"Lost"` (case-insensitive) → navigate('/leads?status=pending_lost')
  With this:
  ```
  If entity_type === 'leads' AND type === 'automation' → navigate('/leads?status=pending_lost')
  If entity_type === 'leads' AND type !== 'automation' → navigate('/leads?view=' + entity_id)

  ```
  ---